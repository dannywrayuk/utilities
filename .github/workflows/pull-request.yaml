name: pull-request
on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 2
      - run: node ./.github/actions/diffToPackageList.js $(git diff --name-only main HEAD)
